# 🎓 Туториал для новых пользователей

## Обзор

Интерактивный туториал автоматически запускается при первом посещении системы и проводит пользователя по основным функциям.

## Архитектура

### Используемые технологии
- **driver.js** - библиотека для создания интерактивных туториалов
- **localStorage** - для отслеживания прохождения туториала
- **data-tutorial** атрибуты - для маркировки элементов UI

### Структура файлов

```
lib/tutorial/
  └── tutorial-config.ts          # Конфигурация шагов туториала

components/tutorial/
  ├── TutorialProvider.tsx         # Провайдер туториала
  └── TutorialButton.tsx           # Кнопка перезапуска

app/
  └── tutorial.css                 # Кастомные стили
```

## Текущие шаги туториала

1. **Приветствие** - Общее введение в систему
2. **Навигация** - Обзор разделов приложения
3. **Доска публикаций** - Объяснение канбан-доски
4. **Создание поста** - Кнопка создания нового контента
5. **Вкладки редактора** - Основное, Полная статья, Медиа, Площадки
6. **Вкладка "Площадки"** - AI адаптация под соцсети
7. **Кнопка "Сгенерировать всё"** - Массовая генерация
8. **Действия с текстом** - Копирование, редактирование, тест, AI
9. **Расписание** - Автоматическая публикация
10. **Завершение** - Финальное сообщение

## 🔧 Как добавить новый шаг

### Шаг 1: Добавить data-tutorial атрибут к элементу

```tsx
<Button data-tutorial="my-new-feature">
  Новая функция
</Button>
```

### Шаг 2: Добавить шаг в конфигурацию

Отредактируйте `lib/tutorial/tutorial-config.ts`:

```typescript
export const TUTORIAL_STEPS: DriveStep[] = [
  // ... существующие шаги
  
  {
    element: '[data-tutorial="my-new-feature"]',
    popover: {
      title: '✨ Новая функция',
      description: `
        <div class="space-y-2">
          <p>Описание новой функции</p>
          <ul class="list-disc list-inside text-sm">
            <li>Пункт 1</li>
            <li>Пункт 2</li>
          </ul>
        </div>
      `,
      side: 'bottom',  // top, bottom, left, right
      align: 'start'   // start, center, end
    }
  }
]
```

### Шаг 3: Проверить работу

1. Очистить localStorage: `localStorage.removeItem('tutorial_completed')`
2. Обновить страницу
3. Туториал запустится автоматически

## 📝 Примеры добавления шагов

### Простой шаг без подсветки элемента

```typescript
{
  popover: {
    title: 'Информация',
    description: 'Просто текст без привязки к элементу',
    side: 'bottom',
    align: 'center'
  }
}
```

### Шаг с подсветкой и списком

```typescript
{
  element: '[data-tutorial="filters"]',
  popover: {
    title: '🔍 Фильтры',
    description: `
      <div class="space-y-2">
        <p>Используйте фильтры для поиска:</p>
        <ul class="list-disc list-inside text-sm space-y-1">
          <li><strong>По дате</strong> - выберите период</li>
          <li><strong>По статусу</strong> - черновик, опубликовано</li>
          <li><strong>По платформе</strong> - VK, Telegram, и др.</li>
        </ul>
      </div>
    `,
    side: 'right',
    align: 'start'
  }
}
```

### Шаг с эмодзи и акцентами

```typescript
{
  element: '[data-tutorial="ai-button"]',
  popover: {
    title: '🤖 AI Генерация',
    description: `
      <div class="space-y-2">
        <p class="font-semibold">Мощь искусственного интеллекта!</p>
        <p class="text-sm text-muted-foreground">
          Нажмите эту кнопку, чтобы AI создал уникальный текст для вашей публикации.
        </p>
      </div>
    `,
    side: 'left',
    align: 'center'
  }
}
```

## 🎨 Стилизация

Стили туториала находятся в `app/tutorial.css`. Используются Tailwind классы для консистентности:

```css
.driver-popover {
  @apply bg-background border border-border shadow-2xl rounded-xl;
}

.driver-popover-title {
  @apply text-lg font-bold text-foreground mb-2;
}
```

## 🔄 Управление туториалом

### Запустить туториал вручную

```typescript
import { startTutorial } from '@/components/tutorial/TutorialProvider'

// В любом компоненте
<Button onClick={startTutorial}>
  Показать туториал
</Button>
```

### Сбросить состояние

```typescript
import { resetTutorial } from '@/lib/tutorial/tutorial-config'

resetTutorial()
startTutorial()
```

### Проверить, проходил ли пользователь

```typescript
import { hasSeenTutorial } from '@/lib/tutorial/tutorial-config'

if (!hasSeenTutorial()) {
  // Показать подсказку
}
```

## 🚀 Автоматизация через агента

Для добавления нового шага через AI агента:

1. **Опишите функцию**: "Добавь шаг туториала для кнопки экспорта"
2. **Агент выполнит**:
   - Добавит `data-tutorial="export-button"` к элементу
   - Создаст новый объект в `TUTORIAL_STEPS`
   - Напишет описание на русском языке
   - Подберёт подходящие эмодзи и форматирование

### Пример команды для агента

```
Добавь шаг туториала для новой кнопки "Экспорт в Excel" 
(data-tutorial="export-excel"). Объясни, что она экспортирует 
все публикации в Excel файл для анализа.
```

## 📊 Статистика

Текущее состояние:
- ✅ 10 шагов туториала
- ✅ 6 элементов с data-tutorial атрибутами
- ✅ Автоматический запуск при первом посещении
- ✅ Кнопка перезапуска в навигации
- ✅ Поддержка темной темы

## 🐛 Отладка

### Туториал не запускается

1. Проверьте консоль на ошибки
2. Убедитесь, что `TutorialProvider` обернут вокруг приложения
3. Очистите localStorage: `localStorage.clear()`

### Элемент не подсвечивается

1. Проверьте, что `data-tutorial` атрибут присутствует в DOM
2. Используйте DevTools для поиска: `document.querySelector('[data-tutorial="element-id"]')`
3. Убедитесь, что селектор в конфигурации совпадает

### Стили не применяются

1. Проверьте импорт `tutorial.css` в `globals.css`
2. Убедитесь, что Tailwind классы компилируются
3. Проверьте порядок импортов CSS

## 📚 Дополнительные ресурсы

- [Driver.js Documentation](https://driverjs.com/)
- [Tailwind CSS](https://tailwindcss.com/)
- [React Best Practices](https://react.dev/)

---

**Последнее обновление**: 2026-02-07  
**Версия**: 1.0.0
