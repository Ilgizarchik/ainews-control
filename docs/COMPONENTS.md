# Библиотека компонентов (UI Inventory)

Этот документ предназначен для AI-агента и разработчиков, чтобы понимать, какие компоненты уже реализованы и готовы к использованию.

## 🧱 Базовые компоненты (Shadcn UI + Custom)

Все базовые UI-компоненты находятся в `@/components/ui`.

- **Кнопки и ввод**:
  - `button.tsx` — Стандартные кнопки с вариантами (ghost, outline, destructive).
  - `input.tsx`, `textarea.tsx` — Поля ввода.
  - `select.tsx` — Выпадающие списки.
  - `switch.tsx` — Переключатели.
  - `label.tsx` — Подписи к полям.

- **Отображение данных и контейнеры**:
  - `card.tsx` — Карточки для контента.
  - `badge.tsx` — Цветные индикаторы статусов.
  - `skeleton.tsx` — Состояния загрузки.
  - `tabs.tsx` — Вкладки.
  - `dialog.tsx` — Модальные окна.
  - `hover-card.tsx` — Всплывающие подсказки при наведении.
  - `tooltip.tsx` — Стандартные тултипы.

- **Сложные элементы**:
  - `calendar.tsx` — Календарь для выбора дат.
  - `popover.tsx` — Всплывающие окна.
  - `rich-editor.tsx` — **ВАЖНО**: Полноценный WYSIWYG редактор (Tiptap) с поддержкой кастомных тегов.

---

## 🏗 Высокоуровневые компоненты (Бизнес-логика)

Находятся в `@/components/publications` и корне `components/`.

- **Общие**:
  - `AppShell.tsx` — Основной макет приложения с сайдбаром.
  - `SideNav.tsx` — Навигационная панель.
  - `StatusBadge.tsx` — Специализированный бейдж для статусов новостей (карта цветов статусов здесь).
  - `ThemeToggle.tsx` — Переключатель темной/светлой темы.

- **Редакторы и модалки**:
  - `NewsEditorDialog.tsx` — Редактор основной новости (лонгрид, анонс).
  - `SocialPostEditorDialog.tsx` — Сложный редактор для постов в соцсети (поддержка мультиплатформенности).
  - `CreatePostDialog.tsx` — Создание нового поста с нуля.
  - `magic-text-editor.tsx` — Обёртка над редактором для ИИ-функций.
  - `system-prompts-editor.tsx` — Интерфейс управления промптами в БД.

- **Представления (Views)**:
  - `BoardView.tsx` — Канбан-доска для управления статусами.
  - `CalendarView.tsx` — Календарное представление сетки публикаций.
  - `DraftsView.tsx` — Список черновиков.
  - `PlatformAnnouncesTab.tsx` — Специальная вкладка для работы с анонсами под разные площадки.
  - `ScheduleTab.tsx` — Планировщик времени публикации.

---

## 🎨 Дизайн-система и иконки

- **Иконки**: Используется `lucide-react`. Всегда указывай размер: `<IconComponent className="w-4 h-4" />`.
- **Цвета**: Прописаны в `tailwind.config.ts` и `app/globals.css`. Используются CSS-переменные для поддержки темной темы.
- **Утилиты**: Используй `cn(...)` из `@/lib/utils` для объединения Tailwind-классов.

## 🛠 Hooks

- `use-media-query.ts` — Для адаптивной верстки.
- `use-toast.ts` — Для уведомлений (хотя проект переходит на `sonner`).

---

**Инструкция для агента**:
> Перед созданием нового компонента, проверь, нет ли его в списке выше. Если нужно создать новую модалку или форму, используй существующие `dialog.tsx`, `button.tsx` и `input.tsx`. Для редактирования текстов ВСЕГДА используй `rich-editor.tsx`.
